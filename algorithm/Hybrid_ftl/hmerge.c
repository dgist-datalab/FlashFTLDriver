#include "../../include/container.h"
#include "hybridmap.h"
extern algorithm hybrid_ftl;
void hybrid_merge(lb_t logblock){
/*    blockmanager *bm = hybrid_ftl.bm;
    bool switch = true;
    for(int j=0;j<_PPS;j++) {
        if (bm->is_invalid_page(logblock[i].lbmapping[j])) { //validÇÑ page¸¦ Å½»ö
            switch = false;
            break;
        }
    }
    if(switch)
        do_switch(logblock);
    else
        partial_merge(logblock)*/;
}


